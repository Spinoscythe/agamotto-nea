package com.srikrishnanethi.agamotto.entities.enums;

/**
 * Outcome of placing a task into a schedule (scheduled, delayed, or excluded).
 */
public enum BlockDecision {
	SCHEDULED,
	DELAYED,
	EXCLUDED
}
